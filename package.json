{
    "name": "node-progress-bar",
    "version": "1.0.0",
    "description": "POC of client side progress bar when downloading a very large dataset from MongoDB via a NodeJS server",
    "main": "main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {},
    "dependencies": {
        "mongodb": "^3.5.7"
    }
}
